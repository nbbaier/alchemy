{
  "name": "fly-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "docker build -t registry.fly.io/alchemy-fly-example:latest .",
    "deploy": "bun run --env-file ../../.env ./alchemy.run.ts",
    "destroy": "bun run --env-file ../../.env ./alchemy.run.ts --destroy",
    "dev": "node src/index.js"
  },
  "dependencies": {
    "alchemy": "workspace:*",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}